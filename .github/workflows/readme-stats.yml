name: Generate GitHub Stats

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Create assets folder
        run: mkdir -p assets

      - name: Generate main stats
        run: |
          curl -L "https://streak-stats.demolab.com?user=MarcMunhoz&theme=vue-dark" \
          -o assets/github-stats.svg

      - name: Generate top languages
        run: |
          curl -L "https://github-readme-stats.vercel.app/api/top-langs/?username=MarcMunhoz&layout=compact&theme=shadow_red" \
          -o assets/top-langs.svg

      - name: Commit updated SVGs
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Update GitHub stats"
          file_pattern: assets/*.svg
